machine:
  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circleci-docker.sh | bash -s -- 1.10.0
  services:
    - docker

docker:
  override:
    - docker build -t gps-test:1.0.0 .

test:
  override:
    - docker run --name gps -d gps-test:1.0.0 node_modules/.bin/_mocha